pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh 'echo "yasir"'
      }
    }

  }
  environment {
    AZURE_LOGIN = 'yasirm@visur.one'
    AZURE_PASSWORD = 'V!sur2021'
  }
}